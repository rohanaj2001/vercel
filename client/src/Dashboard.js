import * as React from "react";
import { Card, CardContent, CardHeader } from '@mui/material';

export default () => (

    <Card>
        <CardHeader title="Welcome to the admin dashboard" />
        <CardContent>You can add new patients, edit existing patient information, and make appointments.</CardContent>
    </Card>
)